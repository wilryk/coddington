"""Coefficient-space flux accumulation prototype (docs/ui-spec-v0.2.md, B2).

Not part of the ``heliostat`` package -- this is a standalone experimental
prototype living entirely under ``scripts/coeff_prototype/``. Nothing under
``src/`` is imported for modification, only read: every module here imports
``heliostat.*`` the way ``scripts/cone_benchmark.py`` does (see each
module's own ``sys.path`` bootstrap when run standalone).
"""
