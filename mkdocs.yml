site_name: Coddington
site_description: "Coddington: a twisting heliostat software package — field layouts, ray tracing, flux maps and annual energy"
repo_url: "https://github.com/wilryk/coddington"

theme:
  name: material
  features:
    - navigation.sections
    - navigation.top
    - search.suggest
    - content.code.copy
  palette:
    - media: "(prefers-color-scheme: light)"
      scheme: default
      toggle: { icon: material/weather-night, name: Dark mode }
    - media: "(prefers-color-scheme: dark)"
      scheme: slate
      toggle: { icon: material/weather-sunny, name: Light mode }

markdown_extensions:
  # Lets the docs site include the top-level REFERENCES.md verbatim,
  # so the canonical file stays at the repo root where it is visible.
  - pymdownx.snippets:
      base_path: ["."]
      check_paths: true

plugins:
  - search
  - mkdocstrings:
      handlers:
        python:
          options:
            docstring_style: sphinx
            show_source: true

nav:
  - Home: index.md
  - Concepts: guide.md
  - Reproducing the paper: paper.md
  - API reference: api.md
  - References: references.md
